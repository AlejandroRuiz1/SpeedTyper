#ifndef UTILITIES_HPP
#define UTILITIES_HPP

#include <vector>
#include <string>

class Utilities {
    public:
        static std::vector<std::string> splitIntoWords(const std::string& sentence);
};

#endif